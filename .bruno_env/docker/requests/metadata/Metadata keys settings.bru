meta {
  name: Metadata keys settings
  type: http
  seq: 2
}

get {
  url: {{host}}/metadata/keys/settings.json
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{jwt_token}}
  Accept: application/json
}
