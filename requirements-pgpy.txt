# PGPy-based implementation requirements
# For environments without GPG binary availability

# Core dependencies (same as main requirements.txt)
requests>=2.32.0
python-dotenv>=1.0.0
tabulate>=0.9.0
PyYAML>=6.0

# PGPy: Pure Python OpenPGP implementation
# No system GPG binary required
pgpy>=0.6.0

# Python 3.13 compatibility (replaces removed imghdr module)
standard-imghdr>=3.13.0

# Optional: cryptography backend for PGPy (better performance)
cryptography>=41.0.0


